/**
 * Admin-only Question System browser entry.
 * The admin Math Lab is a privileged interface and is intentionally separated
 * from the authenticated student browser boundary.
 */
export { default } from "./public-browser.js";
